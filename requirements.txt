# Runtime dependencies of the packaged Windows app.
#
# Declared here rather than only in the CI workflow so the build and a local
# source run install the same set. Pinned because an unpinned build is not
# reproducible, and a desktop app that changes behaviour between two builds of
# the same tag is not debuggable.

# Native window host. Pulls in pythonnet/clr_loader and the WebView2 runtime.
pywebview==5.3.2

# The analyst. Imported lazily at the call site, so PyInstaller cannot see it
# by static analysis - it is also named in the spec's hiddenimports.
# 1.x is the current major (built on httpx2). analyst.py is compatible: it uses
# output_config and adaptive thinking, and passes fallbacks/betas only to the
# beta endpoint, so none of the 0.x-era shapes are in play.
anthropic==1.2.0

# Broker bridge, read-only. Windows-only: the marker keeps `pip install -r`
# working on Linux and macOS for development and CI test runs.
#
# Pulls in numpy, which is why numpy must NOT appear in the spec's excludes.
MetaTrader5==5.0.4874 ; sys_platform == "win32"
